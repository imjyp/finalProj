<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.inventory.jy.mapper.InventoryMapperSG">

    <!-- 재고 기록 목록 조회 -->
    <select id="getInventoryList" resultType="kr.or.ddit.vo.InventoryRecordVO">
        SELECT 
            INVNTRY_RECORD_NO,
            ITEM_NO,
            RECORD_TY,
            RECORD_AMOUNT,
            RECORD_REG_DATE,
            RECORD_ITEM_PRICE,
            STORE_ORDER_NO,
            HEAD_ORDER_NO,
            EXP_DATE
        FROM 
            TEAM3_202407F.INVNTRY_RECORD
        ORDER BY 
            INVNTRY_RECORD_NO DESC
    </select>

    <!-- 재고 기록 추가 -->
    <insert id="insertInventoryRecord" parameterType="kr.or.ddit.vo.InventoryRecordVO">
        INSERT INTO TEAM3_202407F.INVNTRY_RECORD (
            INVNTRY_RECORD_NO,
            ITEM_NO,
            RECORD_TY,
            RECORD_AMOUNT,
            RECORD_REG_DATE,
            RECORD_ITEM_PRICE,
            STORE_ORDER_NO,
            HEAD_ORDER_NO,
            EXP_DATE
        ) VALUES (
            #{invntryRecordNo},
            #{itemNo},
            #{recordTy},
            #{recordAmount},
            #{recordRegDate},
            #{recordItemPrice},
            #{storeOrderNo},
            #{headOrderNo},
            #{expDate}
        )
    </insert>

</mapper>